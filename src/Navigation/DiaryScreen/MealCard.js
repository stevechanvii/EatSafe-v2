import React from 'react';
import { StyleSheet, TouchableOpacity } from 'react-native';
import { Container, Text, Thumbnail, Content, Button, Input, Icon, Item, Form } from 'native-base';

const mealCard = () => {
    return (
        <Text>hello</Text>
    );
};

export default mealCard;